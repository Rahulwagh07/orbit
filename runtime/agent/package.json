{
  "name": "@repo/runtime-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node -e \"console.log('[runtime-agent] runs inside the container (Xvfb/ffmpeg/xdotool live there), not on the host. Build: bun run build && docker build -f runtime/agent/Dockerfile -t orbit-chromium .')\"",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "express": "^4.19.0",
    "mediabunny": "^1.53.1",
    "werift": "0.24.3",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.15.3",
    "@types/ws": "^8.5.10",
    "tsx": "^4.23.8",
    "typescript": "5.9.2"
  }
}