{
  "name": "@repo/db",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "postinstall": "prisma generate",
    "migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7",
    "@prisma/client": "^7",
    "pg": "^8",
    "@repo/env": "*"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "prisma": "^7"
  }
}
